<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MealPlanner.ViewModels"
             xmlns:controls="clr-namespace:MealPlanner.Controls"
             Title="{Binding Title}" 
             x:Class="MealPlanner.Views.TestPage">

    <ContentPage.BindingContext>
        <vm:TestViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <controls:CustomTabView ItemsSource="{Binding Test}" SliderColor="Red">
                <controls:CustomTabView.ContentItemTemplate>
                    <DataTemplate>
                        <Label Style="{StaticResource Label15Bold}" Text="{Binding .}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" LineBreakMode="TailTruncation"/>
                    </DataTemplate>
                </controls:CustomTabView.ContentItemTemplate>
            </controls:CustomTabView>
            <Button Command="{Binding TestCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>