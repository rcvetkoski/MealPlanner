<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MealPlanner.ViewModels"
             xmlns:controls="clr-namespace:MealPlanner.Controls" 
             xmlns:rsControls="clr-namespace:Xamarin.RSControls.Controls;assembly=Xamarin.RSControls" xmlns:enums="clr-namespace:MealPlanner.Helpers.Enums"
             x:Class="MealPlanner.Views.FoodPage"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:FoodViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView Orientation="Vertical">
            <StackLayout Margin="10">

                <rsControls:RSEntry x:Name="Name" Placeholder="Name" Text="{Binding Name}" FontAttributes="Bold" TextColor="Black"/>

                <!--<Grid Margin="10" VerticalOptions="FillAndExpand">
                <Frame Grid.Row="0" Grid.Column="0" Margin="10,0,5,5" CornerRadius="8">
                    <Label Text="200 g"/>
                </Frame>

                <Frame Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Margin="5,0,10,5" CornerRadius="8">
                    <Label Text="356 Cal"/>
                </Frame>

                <Frame Grid.Row="1" Grid.Column="0" Margin="10,0,5,5" CornerRadius="8">
                    <Label Text="Prot 40 g"/>
                </Frame>

                <Frame Grid.Row="2" Grid.Column="1" Margin="5,0,10,5" CornerRadius="8">
                    <Label Text="Carbs 154 g"/>
                </Frame>

                <Frame Grid.Row="2" Grid.Column="0" Margin="10,0,5,5" CornerRadius="8">
                    <Label Text="Fats 23 g"/>
                </Frame>
            </Grid>-->

                <rsControls:RSNumericEntry x:Name="Portion" Placeholder="Portion" Text="{Binding Portion}" CustomUnit="g"/>
                <rsControls:RSEnumPicker x:TypeArguments="enums:AlimentUnitEnum" Placeholder="Unit" SelectedItem="{Binding Unit}"/>
                <rsControls:RSNumericEntry x:Name="Calories" Placeholder="Calories" Text="{Binding Calories}"/>
                <rsControls:RSNumericEntry x:Name="Proteins" Placeholder="Proteins" Text="{Binding Proteins}"/>
                <rsControls:RSNumericEntry x:Name="Carbs" Placeholder="Carbs" Text="{Binding Carbs}"/>
                <rsControls:RSNumericEntry x:Name="Fats" Placeholder="Fats" Text="{Binding Fats}"/>


                <Grid ColumnDefinitions="100, *" RowDefinitions="100, Auto" Margin="15">
                    <controls:CircleCountdown 
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"
                    Grid.Row="0" Grid.Column="0"
                    Progress="{Binding CaloriesProgress}"
                    ProgressStartColor="LightGreen"
                    ProgressEndColor="LightGreen"
                    StrokeWidth="10"/>

                    <StackLayout Grid.Row="0" Grid.Column="0" VerticalOptions="Center">
                        <Label HorizontalOptions="Center" Text="{Binding Calories}" FontAttributes="Bold"/>
                        <Label HorizontalOptions="Center" Text="of"/>
                        <Label HorizontalOptions="Center" Text="{Binding RefData.User.TargetCalories}"/>
                    </StackLayout>

                    <Label Grid.Row="1" Grid.Column="0" HorizontalOptions="Center" Text="Calories"/>

                    <Frame Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" BackgroundColor="White" Margin="4, 4, 4, 15" BorderColor="#f7f7f7" CornerRadius="12" Padding="0">
                        <Grid Margin="10" ColumnDefinitions="Auto, *, Auto, Auto">

                            <Label Grid.Row="0" Grid.Column="0" Text="Prot" VerticalOptions="Center"/>
                            <Label Grid.Row="1" Grid.Column="0" Text="Carbs" VerticalOptions="Center"/>
                            <Label Grid.Row="2" Grid.Column="0" Text="Fats" VerticalOptions="Center"/>

                            <ProgressBar Grid.Row="0" Grid.Column="1" Progress="{Binding ProteinsProgress}" VerticalOptions="Center"/>
                            <ProgressBar Grid.Row="1" Grid.Column="1" Progress="{Binding CarbsProgress}" VerticalOptions="Center"/>
                            <ProgressBar Grid.Row="2" Grid.Column="1" Progress="{Binding FatsProgress}" VerticalOptions="Center"/>

                            <Label Grid.Row="0" Grid.Column="2" VerticalOptions="Center" HorizontalOptions="End">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding Proteins}"/>
                                        <Span Text="   /"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label Grid.Row="0" Grid.Column="3" VerticalOptions="Center" HorizontalOptions="End" Text="{Binding RefData.User.TargetProteins}"/>
                            <Label Grid.Row="1" Grid.Column="2" VerticalOptions="Center" HorizontalOptions="End">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding Carbs}"/>
                                        <Span Text="   /"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label Grid.Row="1" Grid.Column="3" VerticalOptions="Center" HorizontalOptions="End" Text="{Binding RefData.User.TargetCarbs}"/>
                            <Label Grid.Row="2" Grid.Column="2" VerticalOptions="Center" HorizontalOptions="End">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding Fats}"/>
                                        <Span Text="   /"/>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label Grid.Row="2" Grid.Column="3" VerticalOptions="Center" HorizontalOptions="End" Text="{Binding RefData.User.TargetFats}"/>
                        </Grid>
                    </Frame>
                </Grid>


                <Button BackgroundColor="LightGreen" HorizontalOptions="Center" VerticalOptions="EndAndExpand" Text="Save" Command="{Binding SaveCommand}"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>